<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gautam.projectsdl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchChild"></activity>
        <activity android:name=".myGotoLogin" />
        <activity android:name=".BarC" />
        <activity android:name=".PieC" />
        <activity android:name=".Pie_Chart" />
        <activity android:name=".Search" />
        <activity android:name=".DisplayFamInfo" />
        <activity android:name=".InfoAdded" />
        <activity android:name=".FamInformation" />
        <activity android:name=".FunctionalActivity" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Information" />
        <activity android:name=".DisplayData" />
        <activity android:name=".SecondCalc" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity">

            <!-- <activity -->
            <!-- android:name=".ui.login.LoginActivity" -->
            <!-- android:label="@string/title_activity_login" > -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>